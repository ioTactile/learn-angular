services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: habits
      POSTGRES_USER: ${DB_USER:-habits}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - habits_pg:/var/lib/postgresql/data

volumes:
  habits_pg:
