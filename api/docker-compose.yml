services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: habits
      POSTGRES_USER: habits
      POSTGRES_PASSWORD: habits
    ports:
      - "5432:5432"
    volumes:
      - habits_pg:/var/lib/postgresql/data

volumes:
  habits_pg:
